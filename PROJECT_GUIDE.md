# راهنمای پروژه‌ی Outlet Möbel — سند زنده‌ی وضعیت پروژه

> **این فایل رو کامل کپی کن و اولین پیام هر چت جدید با Claude بفرست** تا فوراً همه‌چیز رو بفهمه و بدون سؤال اضافه شروع کنه.
>
> **قانون مهم برای هر چت:** در پایان کار، این فایل رو با تغییرات جدید به‌روزرسانی کن و commit/push کن.

**آخرین به‌روزرسانی:** ۱۸ ژوئیه ۲۰۲۶

---

## ۱. معرفی پروژه

| | |
|---|---|
| **نام** | فروشگاه آنلاین Outlet Möbel GmbH — مبلمان عمده/خرده‌فروشی |
| **مشتری واقعی** | Outlet Möbel GmbH، Weckbacherstrasse 12, 63937 Weilbach، آلمان |
| **مخزن گیت‌هاب** | https://github.com/AS228630/outlet-moebel-shop (عمومی) |
| **دامنه‌ی زنده (dev)** | moebel-shop.vercel.app (پیش‌نمایش — هنوز به دامنه‌ی رسمی وصل نشده) |
| **وضعیت** | در حال توسعه، **هنوز Live نشده** — طراحی تقریباً کامل، بک‌اند/پرداخت/دیتابیس هنوز پیاده نشده |

این پروژه **کاملاً جدا** از پروژه‌ی CertCoach است — کد جدا، مخزن گیت‌هاب جدا. هر دو پروژه هم‌زمان در حال توسعه‌ان.

---

## ۲. پشته‌ی فنی

| بخش | فناوری |
|---|---|
| فریم‌ورک | Next.js 16 (App Router) + React + TypeScript |
| استایل | Tailwind CSS v4 (CSS-first config در `app/globals.css`) |
| کامپوننت‌ها | سبک shadcn/ui، دستی ساخته شده (CLI نیاز به دسترسی اینترنتی داشت که در sandbox موجود نبود) |
| انیمیشن | Framer Motion |
| آیکون‌ها | Lucide React (نسخه‌ی نصب‌شده آیکون‌های برند مثل Facebook/WhatsApp رو نداره — با SVG دستی جایگزین شدن) |
| فونت‌ها | Manrope (تیتر) + Inter (متن) — self-hosted از طریق `@fontsource`، نه Google Fonts |
| هاستینگ | Vercel |
| **دیتابیس/بک‌اند/پرداخت** | **هنوز پیاده نشده** — فعلاً فقط فرانت‌اند استاتیک با داده‌ی نمونه |

---

## ۳. هویت بصری — منبع تصمیم‌گیری‌شده

رنگ‌ها و لوگو **از خودِ عکس‌های واقعی که مالک پروژه فرستاده** استخراج و نمونه‌برداری پیکسلی شدن (نه حدس):

| توکن | مقدار | منبع |
|---|---|---|
| رنگ اصلی (قرمز) | `#CC0207` تا `#D91F26` (چند بار دقیق‌تر شد) | نمونه‌برداری پیکسلی از اسکرین‌شات‌های واقعی سایت مرجع |
| پس‌زمینه‌ی هدر | `#000000` (مشکی خالص) | نمونه‌برداری پیکسلی |
| نوار منو (CategoryNav) | `#131513` (تقریباً مشکی، **نه قرمز کامل**) با فقط آیتم فعال قرمز | نمونه‌برداری پیکسلی — این یه اشتباه رایج بود که چند بار اصلاح شد |
| لوگو | فایل رسمی که مالک پروژه مستقیم فرستاد (`public/images/logo.png`) — crop و پردازش‌شده، **نه بازسازی SVG دستی** (چند بار امتحان شد و اشتباه بود) |
| متن «OUTLET MÖBEL» کنار لوگو | قرمز `#BB282F` / خاکستری `#C3C3C2` — نمونه‌برداری از خودِ فایل لوگوی رسمی |

**درس مهم:** برای این پروژه، تفسیر دستی رنگ/شکل از روی توضیح متنی چندبار اشتباه از آب دراومد. **همیشه اگه عکس واقعی در دسترسه، پیکسل‌هاش رو مستقیم با Python/Pillow نمونه‌برداری کن**، حدس نزن.

---

## ۴. عکس‌ها — وضعیت و هشدار کپی‌رایت

- عکس Hero فعلی: از پست اینستاگرام واقعی خودِ مشتری (`@outletmobel_gmbh`) گرفته شده — مشکلی نداره.
- بقیه‌ی عکس‌هایی که کاربر اولیه فرستاد (برای انتخاب عکس Hero) اکثراً **واترمارک شرکت‌های دیگه** داشتن (احتمالاً از کاتالوگ عمده‌فروشی مثل Alibaba) — **این‌ها هرگز نباید مستقیم رو سایت زنده استفاده بشن**.
- عکس‌های دسته‌بندی (Schlafzimmer, Küche, و بقیه): فعلاً فقط **گرادیانت رنگی جایگزین** دارن، نه عکس واقعی — باید با عکس واقعی محصولات جایگزین بشن.
- ⚠️ **قبل از Live شدن سایت، باید تمام عکس‌ها با عکس‌های واقعی محصولات/نمایشگاه خودِ Outlet Möbel یا عکس استوک قانونی جایگزین بشن.**

---

## ۵. اطلاعات تماس واقعی — منبع واحد

همه‌جا از `lib/business.ts` می‌خونه (به‌جای تکرار پراکنده در هر کامپوننت):

```
آدرس: Weckbacherstrasse 12, 63937 Weilbach
تلفن اصلی: +49 9373 57 89 916
تلفن دوم: +49 176 222 55 915
موبایل/واتساپ: +49 175 5268479
ایمیل: mail@om-moebel.de
ساعت کاری: Mo-Fr 09:00-18:00، Sa 09:00-16:00
امتیاز گوگل: 4.4 (66 نظر)
```

---

## ۶. ساختار صفحات (تا این لحظه)

- `/` — صفحه‌ی اصلی کامل (Header، Hero، دسته‌بندی‌ها، محصولات، نظرات، فوتر)
- `/wohnzimmer` — تنها صفحه‌ی دسته‌بندی کامل (breadcrumb + فیلتر + گرید محصول) — الگوی نمونه برای بقیه‌ی دسته‌ها
- **۴ صفحه‌ی حقوقی واقعی:** `/impressum`, `/agb`, `/datenschutz`, `/widerruf` — متن کامل نوشته شده، فقط چند فیلد ثبت شرکتی (شماره‌ی ثبت، USt-IdNr، نام مدیرعامل) با `<Fill>` زرد علامت‌گذاری شدن که باید مالک پروژه پر کنه
- **۲۷ صفحه‌ی «به‌زودی»** (از `components/ComingSoonPage.tsx`) — برای هر لینکی که تو منو/فوتر بود ولی صفحه‌ی واقعی نداشت (Schlafzimmer, Küche & Essen, Kontakt, Warenkorb, FAQ, Karriere, Blog, و غیره)

---

## ۷. کامپوننت‌های کلیدی

| فایل | نقش |
|---|---|
| `components/Header.tsx` | هدر — موبایل مشکی ۷۲px با منوی همبرگر، دسکتاپ سفید ۹۰px با سرچ کامل |
| `components/MobileNav.tsx` | منوی کشویی موبایل — از **چپ** باز می‌شه، پس‌زمینه‌ی مشکی |
| `components/CategoryNav.tsx` | نوار دسته‌بندی دسکتاپ — پس‌زمینه‌ی تقریباً مشکی، فقط آیتم فعال قرمز |
| `components/BottomNav.tsx` | نوار پایین ثابت موبایل (Home/Kategorien/Wunschliste/Warenkorb/Konto) |
| `components/Hero.tsx` | بخش اصلی صفحه — شامل پنل خدمات، دو حالت واتساپ (داخل کارت + شناور)، امتیاز گوگل |
| `components/Logo.tsx` | از فایل عکس واقعی لوگو استفاده می‌کنه (`public/images/logo.png`)، نه SVG دستی |
| `components/WhatsAppIcon.tsx` | لوگوی واقعی و قابل‌تشخیص واتساپ (نه آیکون عمومی چت) |
| `components/LegalPageShell.tsx` | چارچوب مشترک صفحات حقوقی + کامپوننت `<Fill>` برای علامت‌گذاری اطلاعات ناقص |
| `lib/business.ts` | منبع واحد اطلاعات تماس واقعی |
| `lib/faqData.ts` | بانک سؤالات آماده برای چت‌بات آینده (فعلاً متوقف — پایین رو ببین) |

---

## ۸. قدم‌های باقی‌مانده (اولویت‌بندی‌شده)

### 🔴 قبل از Live شدن — الزامی
- [ ] پر کردن فیلدهای `<Fill>` تو Impressum (شماره‌ی ثبت شرکت، USt-IdNr، نام دقیق مدیرعامل)
- [ ] مشخص‌شدن هزینه‌ی ارسال، مهلت بازگشت کالا، و روش‌های پرداخت واقعی (تو AGB علامت‌گذاری شده)
- [ ] جایگزینی همه‌ی عکس‌های جایگزین/گرادیانت با عکس‌های واقعی محصولات
- [ ] بررسی حقوقی متن‌های AGB/Datenschutz/Widerruf توسط وکیل قبل از انتشار نهایی

### 🟡 فنی — قدم بعدی توسعه
- [ ] ساخت صفحات دسته‌بندی واقعی برای بقیه‌ی ۶ دسته (فعلاً فقط Wohnzimmer الگو داره)
- [ ] اتصال دیتابیس واقعی (Prisma + PostgreSQL طبق برنامه‌ی اولیه) برای محصولات/سبد خرید/حساب کاربری
- [ ] اتصال Stripe برای پرداخت واقعی
- [ ] احراز هویت کاربر (Auth.js یا Clerk)

### ⏸️ متوقف‌شده — منتظر تصمیم مالک پروژه
- [ ] **چت‌بات هوش مصنوعی FAQ** — زیرساخت و بانک سؤالات اولیه آماده‌ست (`lib/faqData.ts`)، اما متوقف چون:
  - Gemini رایگان: قانوناً برای کاربر اتحادیه‌ی اروپا/آلمان ممنوعه (طبق ToS خودِ گوگل)
  - Azure OpenAI: پلن رایگان دائمی نداره
  - Copilot Studio: از ۲۰۰ دلار در ماه شروع می‌شه
  - نزدیک‌ترین گزینه به «واقعاً رایگان»: **Mistral AI** (فرانسوی/اروپایی) — ولی رسماً فقط برای آزمایش، نه تولید تضمین‌شده
  - **در انتظار:** مالک پروژه با صاحب شرکت صحبت کنه و تصمیم بگیره کدوم سرویس (و بودجه‌ی احتمالی)

---

## ۹. درس‌های مهم این پروژه (برای چت‌های بعدی)

1. **همیشه پیکسل رو مستقیم نمونه‌برداری کن**، نه حدس بزن — چندبار تفسیر رنگ/شکل از روی توضیح اشتباه بود، نمونه‌برداری Python/Pillow همیشه درست بود.
2. **ابزار تست محلی (`wkhtmltoimage`) با Tailwind v4 مشکل داره** — به‌جاش از Chrome واقعی (`~/.cache/puppeteer/chrome/`) استفاده کن.
3. **CSS cascade می‌تونه گمراه‌کننده باشه** — یه‌بار یه قانون سراسری `h1{color: dark}` بدون `@layer base` رو رنگ سفید متن Hero غالب شد. همیشه heading rules رو تو `@layer base` بذار.
4. **کپی‌رایت عکس رو جدی بگیر** — عکس‌های واترمارک‌دار از کاتالوگ عمده‌فروشی هرگز رو سایت زنده استفاده نشن.
5. **هیچ داده‌ی جعلی** — قیمت/هزینه‌ی ارسال/مهلت بازگشت که هنوز تأیید نشده، با `<Fill>` علامت‌گذاری شه، هرگز حدس زده نشه.

---

## نحوه‌ی استفاده از این فایل در چت بعدی

این فایل الان در ریشه‌ی مخزن گیت‌هاب (`PROJECT_GUIDE.md`) قرار داره. برای شروع هر چت جدید، محتواش رو کپی کن و بفرست، یا بگو «PROJECT_GUIDE.md رو از گیت‌هاب بخون». در پایان هر جلسه، Claude باید این فایل رو با کارهای جدید و قدم‌های باقی‌مانده به‌روزرسانی و push کنه.
